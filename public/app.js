const app=document.querySelector('#app');let roomCode=localStorage.ttCode||'', me=localStorage.ttId||'', state=null, keys={};
const icons={rocket:'🚀',mine:'💣',boost:'⚡',shield:'🛡',shock:'⚡'};const esc=s=>String(s).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
async function api(path,body){const r=await fetch('/api/'+path,{method:body?'POST':'GET',headers:body?{'Content-Type':'application/json'}:{},body:body?JSON.stringify(body):undefined});return r.json()}
function welcome(){app.innerHTML=`<section class="hero shell"><div class="logo">TURBO <span>TAKEDOWN</span></div><p>2–8 drivers. Three rounds. One champion.</p><div class="card"><h2>Start your engines</h2><div class="form"><input id="name" maxlength="14" placeholder="Driver name"><button id="create">Create room</button></div><div class="join">or join a crew<br><input id="code" maxlength="4" placeholder="CODE"><button id="join">Join</button></div><p class="small" id="err"></p></div></section>`;document.querySelector('#create').onclick=async()=>{let d=await api('create',{name:document.querySelector('#name').value});enter(d)};document.querySelector('#join').onclick=async()=>{let c=document.querySelector('#code').value.toUpperCase(),d=await api(`room/${c}/join`,{name:document.querySelector('#name').value});if(d.error)document.querySelector('#err').textContent=d.error;else enter(d)}}
function enter(d){roomCode=d.code;me=d.id;localStorage.ttCode=roomCode;localStorage.ttId=me;poll()}
function seconds(){return Math.max(0,Math.ceil((state.roundEnds-Date.now())/1000))}
function render(){if(!state)return;const mine=state.players.find(p=>p.id===me);if(!mine){localStorage.clear();return welcome()}if(state.phase==='lobby')return lobby(mine);if(state.phase==='results'||state.phase==='final')return results();game(mine)}
function lobby(mine){app.innerHTML=`<div class="shell"><div class="top"><div class="logo">TURBO <span>TAKEDOWN</span></div><div class="pill">ROOM ${state.code}</div></div><section class="card lobby"><h1>Garage is open</h1><p>Share code <b>${state.code}</b> with your rivals</p><div class="drivers">${state.players.map(p=>`<div class="driver" style="--c:${p.color}">${esc(p.name)}<br><span class="small">ready to race</span></div>`).join('')}</div>${mine.id===state.host?`<button id="start" ${state.players.length<2?'disabled':''}>Start match</button><p class="small">Need at least 2 drivers</p>`:`<p class="small">Waiting for the host to start…</p>`}</section></div>`;let b=document.querySelector('#start');if(b)b.onclick=()=>api(`room/${roomCode}/start`,{id:me})}
function game(mine){const t=seconds();app.innerHTML=`<div class="shell"><div class="top"><div class="logo">TURBO <span>TAKEDOWN</span></div><div class="pill">ROUND ${state.round}/3 · ${t}s</div></div><div class="arenaWrap"><div class="arena"><div class="track"></div><div class="hazard"></div>${state.crates.map(c=>`<div class="crate" style="left:${c.x/10}%;top:${c.y/7}%;">${icons[c.type]}</div>`).join('')}${state.players.map(p=>`<div title="${esc(p.name)}" class="car ${p.shield>Date.now()?'shield':''}" style="left:${p.x/10}%;top:${p.y/7}%;background:${p.color};opacity:${p.alive?1:.15};transform:translate(-50%,-50%) rotate(${p.a}rad)"></div>`).join('')}</div><div class="hud"><div class="scorebar">${state.players.sort((a,b)=>b.score-a.score).map(p=>`<span style="color:${p.color}">●</span>${p.score} `).join('')}</div><div class="scorebar">${mine.kos} KOs</div></div><div class="power">${mine.alive?(mine.power?`${icons[mine.power]} ${mine.power.toUpperCase()} · SPACE / USE`:'Find a power crate'):'RESPAWNING…'}</div></div><div class="touch"><button data-k="left">◀</button><button data-k="up">▲</button><button id="use">USE</button><button data-k="right">▶</button></div><p class="small">Drive: arrow keys / WASD · Use power: Space</p></div>`;document.querySelector('#use').onclick=use;document.querySelectorAll('[data-k]').forEach(b=>{let k=b.dataset.k;b.onpointerdown=()=>{keys[k]=true};b.onpointerup=b.onpointerleave=()=>{keys[k]=false}})}
function results(){const final=state.phase==='final',rank=[...state.players].sort((a,b)=>b.score-a.score||b.kos-a.kos),winner=rank[0];app.innerHTML=`<div class="shell"><div class="top"><div class="logo">TURBO <span>TAKEDOWN</span></div><div class="pill">${final?'FINAL RESULTS':`ROUND ${state.round} COMPLETE`}</div></div><section class="card results"><h1>${final?`${esc(winner.name)} wins!`:'Checkered flag!'}</h1><p>${final?'Turbo legend status achieved.':'Placement bonuses have been added.'}</p><div class="ranking">${rank.map((p,i)=>`<div class="rank"><span>${i+1}. <b style="color:${p.color}">${esc(p.name)}</b></span><span>${p.score} pts · ${p.kos} KOs</span></div>`).join('')}</div>${!final&&me===state.host?'<button id="next">Next round</button>':!final?'<p class="small">Waiting for the host…</p>':'<button id="again">New room</button>'}</section></div>`;let n=document.querySelector('#next');if(n)n.onclick=()=>api(`room/${roomCode}/next`,{id:me});let a=document.querySelector('#again');if(a)a.onclick=()=>{localStorage.clear();roomCode=me='';welcome()}}
async function poll(){try{state=await api(`room/${roomCode}`);render();if(state.phase==='playing')api(`room/${roomCode}/control`,{id:me,keys});}catch{}setTimeout(poll,250)}
function use(){if(state?.phase==='playing')api(`room/${roomCode}/use`,{id:me})}window.addEventListener('keydown',e=>{const map={ArrowLeft:'left',a:'left',ArrowRight:'right',d:'right',ArrowUp:'up',w:'up',ArrowDown:'down',s:'down'};if(map[e.key.toLowerCase()]){keys[map[e.key.toLowerCase()]]=true;e.preventDefault()}if(e.code==='Space'){use();e.preventDefault()}});window.addEventListener('keyup',e=>{const map={ArrowLeft:'left',a:'left',ArrowRight:'right',d:'right',ArrowUp:'up',w:'up',ArrowDown:'down',s:'down'};if(map[e.key.toLowerCase()])keys[map[e.key.toLowerCase()]]=false});roomCode&&me?poll():welcome();
